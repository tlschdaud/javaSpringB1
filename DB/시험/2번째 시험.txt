12장
1. 다음 열 구조를 가지는 EMP_HW 테이블을 만들어 보세요.
CREATE TABLE EMP_HW (
	EMPNO NUMBER(4),
	ENAME VARCHAR2(10),
	JOB VARCHAR2(9),
	MGR NUMBER(4),
	HIREDATE DATE,
	SAL NUMBER(7,2),
	COMM NUMBER(7,2),
	DEPTNO NUMBER(2)
);


2 EMP_HW 테이블에 BIGO 열을 추가해보세요 . BIGO 열의 자료형은 가변형 문자열이고, 길이는 20입니다.

ALTER TABLE EMP_HW ADD BIGO VARCHAR2(20);

3. EMP_HW 테이블의 BIGO 열 크기를 30으로 변경해 보세요.

ALTER TABLE EMP_HW MODIFY BIGO VARCHAR2(30);

4. EMP_HW 테이블의 BIGO 열 이름을 REMARK로 변경해 보세요.

ALTER TABLE EMP_HW RENAME COLUMN BIGO TO REMARK;

5.